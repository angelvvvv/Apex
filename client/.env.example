# Only needed for production builds (e.g. deploying to Vercel).
# In local dev, Vite's proxy in vite.config.js forwards /api to the server, so this can stay unset.
VITE_API_URL=https://your-render-backend.onrender.com/api
